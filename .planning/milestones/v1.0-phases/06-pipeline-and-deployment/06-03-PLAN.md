---
phase: 06-pipeline-and-deployment
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - docs/DEPLOY.md
  - docs/vps-nginx-block.conf
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04]
must_haves:
  truths:
    - "VPS nginx server block template exists as a standalone file ready to copy"
    - "Deployment guide covers all 9 VPS bootstrap steps from CONTEXT.md"
    - "Deployment guide references the correct port (8080) and domain (nostradamus.silverreyes.net)"
    - "No MLflow or Caddy references remain anywhere in the project (full sweep)"
  artifacts:
    - path: "docs/vps-nginx-block.conf"
      provides: "VPS nginx server block for nostradamus.silverreyes.net"
      contains: "proxy_pass http://localhost:8080"
    - path: "docs/DEPLOY.md"
      provides: "Step-by-step VPS deployment guide"
      contains: "nostradamus.silverreyes.net"
  key_links:
    - from: "docs/vps-nginx-block.conf"
      to: "docker-compose.yml nginx service"
      via: "port 8080 mapping"
      pattern: "localhost:8080"
    - from: "docs/DEPLOY.md"
      to: ".env.example"
      via: "cp .env.example .env instruction"
      pattern: "cp .env.example .env"
---

<objective>
Create VPS deployment artifacts: the nginx server block template file and a step-by-step deployment guide. Then run a full-project sweep to verify zero MLflow/Caddy references remain.

Purpose: The deployment guide is the human-facing artifact that makes the VPS bootstrap repeatable. The nginx server block is the one file the user pastes on the VPS. The final sweep catches any stale references missed by plans 01 and 02.

Output: docs/DEPLOY.md, docs/vps-nginx-block.conf, verified clean codebase.
</objective>

<execution_context>
@E:/ClaudeCodeProjects/GamePredictor/.claude/get-shit-done/workflows/execute-plan.md
@E:/ClaudeCodeProjects/GamePredictor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/06-pipeline-and-deployment/06-CONTEXT.md
@.planning/milestones/v1.0-REQUIREMENTS.md
@.planning/milestones/v1.0-phases/06-pipeline-and-deployment/06-01-SUMMARY.md
@.planning/milestones/v1.0-phases/06-pipeline-and-deployment/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VPS nginx block and deployment guide</name>
  <files>docs/vps-nginx-block.conf, docs/DEPLOY.md</files>
  <read_first>docker-compose.yml, .env.example, docker/entrypoint.sh</read_first>
  <action>
Create the `docs/` directory if it does not exist.

**docs/vps-nginx-block.conf** — create with this exact content:
```nginx
server {
    listen 80;
    server_name nostradamus.silverreyes.net;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
# After adding this block, run: sudo certbot --nginx -d nostradamus.silverreyes.net
```

**docs/DEPLOY.md** — create a deployment guide with these exact 9 steps:

Title: `# VPS Deployment Guide`

Subtitle: `Deploy NFL Game Predictor to a Hostinger VPS at nostradamus.silverreyes.net`

**Prerequisites section:**
- Ubuntu VPS with root/sudo access
- Domain DNS managed via Hostinger panel
- Existing VPS nginx serving other sites (do NOT touch existing server blocks)

**Step 1: Install Docker**
```sh
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
newgrp docker
sudo apt-get install -y docker-compose-plugin
```
Verify: `docker compose version`

**Step 2: Clone repository**
```sh
git clone https://github.com/NatoJenkins/GamePredictor.git
cd GamePredictor
```

**Step 3: Configure environment**
```sh
cp .env.example .env
```
Then edit `.env` and set real values for `POSTGRES_PASSWORD` and `RELOAD_TOKEN`. `REFRESH_CRON_HOUR` defaults to 6 (Tuesday 6 AM UTC).

**Step 4: Get VPS IP for DNS**
```sh
curl -4 ifconfig.me
```
Note the IP address for the next step.

**Step 5: Configure DNS**
In Hostinger DNS panel, add an A record:
- Name: `nostradamus`
- Points to: `<VPS IP from step 4>`
- TTL: default

Wait for DNS propagation before proceeding (check with `dig nostradamus.silverreyes.net`).

**Step 6: Add VPS nginx server block**
```sh
sudo cp docs/vps-nginx-block.conf /etc/nginx/sites-available/nostradamus.silverreyes.net
sudo ln -s /etc/nginx/sites-available/nostradamus.silverreyes.net /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

**Step 7: SSL with Certbot**
```sh
sudo certbot --nginx -d nostradamus.silverreyes.net
```
Certbot modifies the server block to add SSL. The app's nginx container remains plain HTTP internally.

**Step 8: Start services**
```sh
docker compose up -d
```
Services start in order: postgres (init.sql creates schema) -> api (loads model from seeded volume) -> worker (APScheduler) -> nginx (builds frontend).

First build takes several minutes (npm install + pip install).

Verify: `docker compose ps` shows all 4 services healthy.

**Step 9: First data run (interactive)**
```sh
docker compose exec worker python -m pipeline.refresh
```
This is the initial 20-season data ingestion. Takes ~30 minutes. Watch for errors — do NOT background this.

After completion, visit `https://nostradamus.silverreyes.net` to verify the dashboard loads.

**Ongoing operations section:**
- Weekly refresh runs automatically (Tuesday 6 AM UTC via APScheduler in worker)
- After retrain, approve new model: `curl -X POST https://nostradamus.silverreyes.net/api/model/reload -H "Authorization: Bearer <RELOAD_TOKEN>"`
- View logs: `docker compose logs -f worker`
- Rebuild after code changes: `git pull && docker compose up -d --build`
  </action>
  <verify>
    <automated>python -c "
import os
assert os.path.exists('docs/vps-nginx-block.conf'), 'vps-nginx-block.conf missing'
assert os.path.exists('docs/DEPLOY.md'), 'DEPLOY.md missing'
content = open('docs/vps-nginx-block.conf').read()
assert 'proxy_pass http://localhost:8080' in content, 'wrong proxy_pass in vps block'
assert 'nostradamus.silverreyes.net' in content, 'wrong server_name in vps block'
deploy = open('docs/DEPLOY.md').read()
assert 'nostradamus.silverreyes.net' in deploy, 'domain missing from deploy guide'
assert 'docker compose up -d' in deploy, 'docker compose up missing from deploy guide'
assert 'pipeline.refresh' in deploy, 'first data run missing from deploy guide'
assert '.env.example' in deploy, 'env setup missing from deploy guide'
assert 'certbot' in deploy, 'SSL step missing from deploy guide'
assert '8080' in deploy, 'port 8080 missing from deploy guide'
print('Deployment artifacts valid')
"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/vps-nginx-block.conf` exists
    - File `docs/vps-nginx-block.conf` contains `proxy_pass http://localhost:8080;`
    - File `docs/vps-nginx-block.conf` contains `server_name nostradamus.silverreyes.net;`
    - File `docs/DEPLOY.md` exists
    - File `docs/DEPLOY.md` contains `nostradamus.silverreyes.net`
    - File `docs/DEPLOY.md` contains `docker compose up -d`
    - File `docs/DEPLOY.md` contains `python -m pipeline.refresh`
    - File `docs/DEPLOY.md` contains `cp .env.example .env`
    - File `docs/DEPLOY.md` contains `certbot --nginx`
    - File `docs/DEPLOY.md` contains `curl -4 ifconfig.me`
    - File `docs/DEPLOY.md` does NOT contain `mlflow`
    - File `docs/DEPLOY.md` does NOT contain `caddy`
  </acceptance_criteria>
  <done>VPS nginx block template and step-by-step deployment guide created covering all 9 bootstrap steps.</done>
</task>

<task type="auto">
  <name>Task 2: Full-project MLflow/Caddy sweep and final verification</name>
  <files></files>
  <read_first>docker-compose.yml, pyproject.toml</read_first>
  <action>
Run a comprehensive sweep of the entire project to verify zero stale references remain:

1. Search all Python files for `mlflow`: `grep -r "mlflow" --include="*.py" . | grep -v ".planning/" | grep -v "__pycache__/" | grep -v ".pyc"`
   Expected: zero matches

2. Search all YAML/YML files for `mlflow`: `grep -r "mlflow" --include="*.yml" --include="*.yaml" .`
   Expected: zero matches

3. Search all files for `caddy` (case-insensitive): `grep -ri "caddy" --include="*.py" --include="*.yml" --include="*.yaml" --include="*.toml" --include="*.cfg" --include="*.sh" .`
   Expected: zero matches

4. Verify deleted files: `ls mlflow.Dockerfile Caddyfile 2>/dev/null` returns nothing

5. Verify docker-compose.yml service count: parse YAML and confirm exactly 4 service keys (postgres, api, worker, nginx)

6. Verify docker-compose.yml volume count: parse YAML and confirm exactly 2 volume keys (pgdata, models)

7. Verify .env.example has no DOMAIN line

8. Run the test suite to confirm nothing is broken: `python -m pytest tests/ -x --timeout=60`

If ANY of the above checks fail, fix the issue before proceeding. The most likely stale references are:
- Comments mentioning MLflow in docstrings
- `mlruns` in .gitignore or .dockerignore (this is OK to keep — it's just a gitignore pattern)
- The training docstring in models/train.py mentioning "dual logging" or "MLflow"
  </action>
  <verify>
    <automated>python -c "
import subprocess, sys
# Check no mlflow in Python files
result = subprocess.run(['grep', '-r', 'mlflow', '--include=*.py', '.'], capture_output=True, text=True)
py_matches = [l for l in result.stdout.strip().split('\n') if l and '.planning/' not in l and '__pycache__' not in l]
if py_matches:
    print('FAIL: mlflow found in Python files:'); [print(f'  {m}') for m in py_matches]; sys.exit(1)
# Check no mlflow in YAML
result2 = subprocess.run(['grep', '-r', 'mlflow', '--include=*.yml', '--include=*.yaml', '.'], capture_output=True, text=True)
yml_matches = [l for l in result2.stdout.strip().split('\n') if l and '.planning/' not in l]
if yml_matches:
    print('FAIL: mlflow found in YAML files:'); [print(f'  {m}') for m in yml_matches]; sys.exit(1)
# Check no caddy in source files
result3 = subprocess.run(['grep', '-ri', 'caddy', '--include=*.py', '--include=*.yml', '--include=*.yaml', '--include=*.toml', '--include=*.sh', '.'], capture_output=True, text=True)
caddy_matches = [l for l in result3.stdout.strip().split('\n') if l and '.planning/' not in l]
if caddy_matches:
    print('FAIL: caddy found in source files:'); [print(f'  {m}') for m in caddy_matches]; sys.exit(1)
print('PASS: No stale mlflow or caddy references in source files')
" && python -m pytest tests/ -x --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "mlflow" --include="*.py" .` (excluding .planning/ and __pycache__) returns zero matches
    - `grep -r "mlflow" --include="*.yml" .` (excluding .planning/) returns zero matches
    - `grep -ri "caddy" --include="*.py" --include="*.yml" --include="*.toml" --include="*.sh" .` (excluding .planning/) returns zero matches
    - File `mlflow.Dockerfile` does NOT exist
    - File `Caddyfile` does NOT exist
    - `python -m pytest tests/ -x` exits with code 0
  </acceptance_criteria>
  <done>Full project is clean of MLflow and Caddy references. All tests pass. Deployment artifacts are complete.</done>
</task>

</tasks>

<verification>
1. `docs/vps-nginx-block.conf` contains correct server block for nostradamus.silverreyes.net -> localhost:8080
2. `docs/DEPLOY.md` contains all 9 VPS bootstrap steps
3. Zero mlflow/caddy references in source files (Python, YAML, TOML, shell)
4. All tests pass
5. Complete file inventory matches CONTEXT.md specification
</verification>

<success_criteria>
- VPS nginx server block template exists at docs/vps-nginx-block.conf
- Deployment guide exists at docs/DEPLOY.md with all 9 bootstrap steps
- Full project sweep confirms zero stale MLflow/Caddy references
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/06-pipeline-and-deployment/06-03-SUMMARY.md`
</output>
