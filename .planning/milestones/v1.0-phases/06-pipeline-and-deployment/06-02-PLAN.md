---
phase: 06-pipeline-and-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - docker-compose.yml
  - docker/entrypoint.sh
  - docker/nginx.conf
  - docker/nginx.Dockerfile
  - .dockerignore
  - .env.example
autonomous: true
requirements: [PIPE-03, PIPE-04]
must_haves:
  truths:
    - "docker-compose.yml defines exactly 4 services: postgres, api, worker, nginx"
    - "docker-compose.yml defines exactly 2 volumes: pgdata, models"
    - "Dockerfile has ENTRYPOINT that runs entrypoint.sh"
    - "entrypoint.sh seeds models volume if best_model.json is missing"
    - "nginx.Dockerfile builds frontend and serves via nginx"
    - "nginx.conf routes /api/ to api:8000 and / to static files"
    - ".env.example has POSTGRES_PASSWORD, RELOAD_TOKEN, REFRESH_CRON_HOUR and no DOMAIN"
  artifacts:
    - path: "docker/entrypoint.sh"
      provides: "Model volume seeding on first boot"
      contains: "cp /app/models/artifacts/best_model.json"
    - path: "docker/nginx.conf"
      provides: "App nginx routing"
      contains: "proxy_pass http://api:8000"
    - path: "docker/nginx.Dockerfile"
      provides: "Multi-stage frontend build + nginx serve"
      contains: "FROM node:20-alpine AS builder"
    - path: "docker-compose.yml"
      provides: "4-service orchestration"
      contains: "nginx:"
    - path: "Dockerfile"
      provides: "Python app image with entrypoint"
      contains: "ENTRYPOINT"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "RELOAD_TOKEN"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build context for api and worker services"
      pattern: "build: \\."
    - from: "docker-compose.yml"
      to: "docker/nginx.Dockerfile"
      via: "nginx service build"
      pattern: "dockerfile: docker/nginx.Dockerfile"
    - from: "docker/nginx.conf"
      to: "api:8000"
      via: "proxy_pass for /api/ location"
      pattern: "proxy_pass http://api:8000"
    - from: "Dockerfile"
      to: "docker/entrypoint.sh"
      via: "COPY and ENTRYPOINT"
      pattern: "ENTRYPOINT.*entrypoint.sh"
---

<objective>
Create all Docker infrastructure files: update Dockerfile with entrypoint, rewrite docker-compose.yml to 4 services, create docker/entrypoint.sh, docker/nginx.conf, docker/nginx.Dockerfile, update .dockerignore and .env.example.

Purpose: This establishes the containerized deployment stack. The app runs as postgres + api + worker + nginx, with model volume seeding on first boot and multi-stage frontend builds.

Output: Complete Docker infrastructure ready for `docker compose up -d`.
</objective>

<execution_context>
@E:/ClaudeCodeProjects/GamePredictor/.claude/get-shit-done/workflows/execute-plan.md
@E:/ClaudeCodeProjects/GamePredictor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/06-pipeline-and-deployment/06-CONTEXT.md
@.planning/milestones/v1.0-REQUIREMENTS.md

<interfaces>
<!-- Current file states the executor must understand before modifying. -->

From Dockerfile (current — 13 lines):
```dockerfile
FROM python:3.11-slim AS builder
WORKDIR /app
COPY pyproject.toml ./
RUN pip install --no-cache-dir --prefix=/install \
    $(python -c "import tomllib; print(' '.join(tomllib.load(open('pyproject.toml','rb'))['project']['dependencies']))")

FROM python:3.11-slim AS runtime
WORKDIR /app
COPY --from=builder /install /usr/local
COPY . .
ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1
CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

From docker-compose.yml (current — 89 lines, 5 services, 4 volumes):
- postgres: KEEP as-is (lines 2-15)
- api: KEEP structure, no env var changes needed (lines 17-35)
- worker: REMOVE `MLFLOW_TRACKING_URI: http://mlflow:5000` env var (line 42)
- mlflow: DELETE entire service block (lines 52-69)
- caddy: DELETE entire service block (lines 71-82)
- volumes: KEEP pgdata + models, DELETE mlartifacts + caddy_data

From .dockerignore (current — 8 lines):
```
.git
.planning
data/cache
frontend/node_modules
mlruns
__pycache__
*.pyc
.env
```

From .env.example (current — 4 vars):
```
POSTGRES_PASSWORD=changeme_generate_strong_password
RELOAD_TOKEN=changeme_generate_random_token
DOMAIN=localhost
REFRESH_CRON_HOUR=6
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker/ directory files (entrypoint.sh, nginx.conf, nginx.Dockerfile)</name>
  <files>docker/entrypoint.sh, docker/nginx.conf, docker/nginx.Dockerfile</files>
  <read_first>Dockerfile, docker-compose.yml, frontend/package.json</read_first>
  <action>
Create the `docker/` directory if it does not exist.

**docker/entrypoint.sh** — create with this exact content:
```sh
#!/bin/sh
# Seed models volume on first boot if volume is empty
MODEL_VOL="/app/models-vol"
if [ ! -f "$MODEL_VOL/best_model.json" ]; then
    echo "[entrypoint] Seeding models volume from image..."
    cp /app/models/artifacts/best_model.json "$MODEL_VOL/"
    cp /app/models/experiments.jsonl "$MODEL_VOL/"
    echo "[entrypoint] Seeding complete."
fi
exec "$@"
```
IMPORTANT: File must have Unix line endings (LF, not CRLF). Use `\n` not `\r\n`.

**docker/nginx.conf** — create with this exact content:
```nginx
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # API proxy — strip /api prefix, forward to FastAPI
    location /api/ {
        proxy_pass http://api:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }

    # SPA fallback — serve index.html for all non-file routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

**docker/nginx.Dockerfile** — create with this exact content:
```dockerfile
# Stage 1: Build React frontend
FROM node:20-alpine AS builder
WORKDIR /app/frontend
COPY frontend/package.json frontend/package-lock.json ./
RUN npm ci
COPY frontend/ .
RUN npm run build

# Stage 2: Serve with nginx
FROM nginx:alpine
COPY --from=builder /app/frontend/dist /usr/share/nginx/html
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```
  </action>
  <verify>
    <automated>python -c "import os; assert os.path.exists('docker/entrypoint.sh'); assert os.path.exists('docker/nginx.conf'); assert os.path.exists('docker/nginx.Dockerfile'); print('All docker/ files exist')" && grep -c "exec" docker/entrypoint.sh && grep -c "proxy_pass http://api:8000" docker/nginx.conf && grep -c "FROM node:20-alpine AS builder" docker/nginx.Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - File `docker/entrypoint.sh` exists and contains `exec "$@"` as last line
    - File `docker/entrypoint.sh` contains `cp /app/models/artifacts/best_model.json "$MODEL_VOL/"`
    - File `docker/entrypoint.sh` contains `cp /app/models/experiments.jsonl "$MODEL_VOL/"`
    - File `docker/nginx.conf` contains `proxy_pass http://api:8000/api/;`
    - File `docker/nginx.conf` contains `try_files $uri $uri/ /index.html;`
    - File `docker/nginx.conf` contains `listen 80;`
    - File `docker/nginx.Dockerfile` contains `FROM node:20-alpine AS builder`
    - File `docker/nginx.Dockerfile` contains `FROM nginx:alpine`
    - File `docker/nginx.Dockerfile` contains `COPY --from=builder /app/frontend/dist /usr/share/nginx/html`
    - File `docker/nginx.Dockerfile` contains `COPY docker/nginx.conf /etc/nginx/conf.d/default.conf`
  </acceptance_criteria>
  <done>Three docker/ files created with correct content — entrypoint seeds volume, nginx.conf routes API and SPA, nginx.Dockerfile builds frontend in multi-stage.</done>
</task>

<task type="auto">
  <name>Task 2: Update Dockerfile, rewrite docker-compose.yml, update .dockerignore and .env.example</name>
  <files>Dockerfile, docker-compose.yml, .dockerignore, .env.example</files>
  <read_first>Dockerfile, docker-compose.yml, .dockerignore, .env.example</read_first>
  <action>
**Dockerfile** — add 3 lines to the runtime stage (after `COPY . .` and ENV lines, before `CMD`):
```dockerfile
FROM python:3.11-slim AS builder
WORKDIR /app
COPY pyproject.toml ./
RUN pip install --no-cache-dir --prefix=/install \
    $(python -c "import tomllib; print(' '.join(tomllib.load(open('pyproject.toml','rb'))['project']['dependencies']))")

FROM python:3.11-slim AS runtime
WORKDIR /app
COPY --from=builder /install /usr/local
COPY . .
ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1
COPY docker/entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
ENTRYPOINT ["/entrypoint.sh"]
CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]
```
The key additions are lines: `COPY docker/entrypoint.sh /entrypoint.sh`, `RUN chmod +x /entrypoint.sh`, and `ENTRYPOINT ["/entrypoint.sh"]`. These go AFTER the ENV lines and BEFORE the CMD line.

**docker-compose.yml** — complete rewrite with this exact content:
```yaml
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nflpredictor
      POSTGRES_USER: nfl
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nfldev}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./sql:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nfl"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    command: ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]
    environment:
      DATABASE_URL: postgresql+psycopg://nfl:${POSTGRES_PASSWORD:-nfldev}@postgres:5432/nflpredictor
      MODEL_PATH: /app/models-vol/best_model.json
      EXPERIMENTS_PATH: /app/models-vol/experiments.jsonl
      RELOAD_TOKEN: ${RELOAD_TOKEN:-devtoken}
    volumes:
      - models:/app/models-vol:ro
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  worker:
    build: .
    command: ["python", "-m", "pipeline.worker"]
    environment:
      DATABASE_URL: postgresql+psycopg://nfl:${POSTGRES_PASSWORD:-nfldev}@postgres:5432/nflpredictor
      MODEL_PATH: /app/models-vol/best_model.json
      EXPERIMENTS_PATH: /app/models-vol/experiments.jsonl
      REFRESH_CRON_HOUR: ${REFRESH_CRON_HOUR:-6}
    volumes:
      - models:/app/models-vol
    depends_on:
      postgres:
        condition: service_healthy

  nginx:
    build:
      context: .
      dockerfile: docker/nginx.Dockerfile
    ports:
      - "8080:80"
    depends_on:
      api:
        condition: service_healthy

volumes:
  pgdata:
  models:
```
Key changes from current:
- Removed: mlflow service, caddy service, mlartifacts volume, caddy_data volume
- Removed: `MLFLOW_TRACKING_URI: http://mlflow:5000` from worker environment
- Added: nginx service (build from docker/nginx.Dockerfile, port 8080:80, depends_on api healthy)
- Kept: postgres (unchanged), api (unchanged), worker (minus MLFLOW_TRACKING_URI)

**.dockerignore** — rewrite with this exact content:
```
.git
.planning
data/cache
frontend/node_modules
frontend/dist
mlruns
__pycache__
*.pyc
.env
*.md
!frontend/README.md
```
Added `frontend/dist` (build artifacts should not enter Docker context). Added `*.md` exclusion with `!frontend/README.md` exception to reduce context size.

**.env.example** — rewrite with this exact content:
```
# PostgreSQL (single source of truth for password)
POSTGRES_PASSWORD=changeme

# API model reload authorization token
RELOAD_TOKEN=changeme

# Worker schedule (hour in UTC, default 6 = Tuesday 6 AM UTC)
REFRESH_CRON_HOUR=6
```
Key change: removed `DOMAIN=localhost` line (Caddy is gone, nginx does not need it).
  </action>
  <verify>
    <automated>grep -c "ENTRYPOINT" Dockerfile && grep -c "entrypoint.sh" Dockerfile && python -c "
content = open('docker-compose.yml').read()
assert 'mlflow' not in content, 'mlflow still in docker-compose.yml'
assert 'caddy' not in content, 'caddy still in docker-compose.yml'
assert 'mlartifacts' not in content, 'mlartifacts still in docker-compose.yml'
assert 'caddy_data' not in content, 'caddy_data still in docker-compose.yml'
assert 'MLFLOW_TRACKING_URI' not in content, 'MLFLOW_TRACKING_URI still in docker-compose.yml'
assert 'nginx:' in content, 'nginx service missing'
assert '8080:80' in content, 'nginx port mapping missing'
assert content.count('build:') >= 2, 'expected at least 2 build directives'
print('docker-compose.yml valid')
" && python -c "
content = open('.env.example').read()
assert 'DOMAIN' not in content, 'DOMAIN still in .env.example'
assert 'POSTGRES_PASSWORD' in content
assert 'RELOAD_TOKEN' in content
assert 'REFRESH_CRON_HOUR' in content
print('.env.example valid')
" && grep -c "frontend/dist" .dockerignore</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile` contains `ENTRYPOINT ["/entrypoint.sh"]`
    - `Dockerfile` contains `COPY docker/entrypoint.sh /entrypoint.sh`
    - `Dockerfile` contains `RUN chmod +x /entrypoint.sh`
    - `docker-compose.yml` does NOT contain `mlflow`
    - `docker-compose.yml` does NOT contain `caddy`
    - `docker-compose.yml` does NOT contain `mlartifacts`
    - `docker-compose.yml` does NOT contain `caddy_data`
    - `docker-compose.yml` does NOT contain `MLFLOW_TRACKING_URI`
    - `docker-compose.yml` contains `nginx:` as a service
    - `docker-compose.yml` contains `8080:80`
    - `docker-compose.yml` contains `dockerfile: docker/nginx.Dockerfile`
    - `docker-compose.yml` contains exactly 2 entries under `volumes:` at root level: `pgdata:` and `models:`
    - `.env.example` does NOT contain `DOMAIN`
    - `.env.example` contains `POSTGRES_PASSWORD`
    - `.env.example` contains `RELOAD_TOKEN`
    - `.env.example` contains `REFRESH_CRON_HOUR`
    - `.dockerignore` contains `frontend/dist`
  </acceptance_criteria>
  <done>Dockerfile has entrypoint, docker-compose.yml has 4 services and 2 volumes with no MLflow/Caddy references, .env.example has 3 vars (no DOMAIN), .dockerignore includes frontend/dist.</done>
</task>

</tasks>

<verification>
1. `docker compose config` validates the compose file syntax (run only if docker is available)
2. `grep -c "mlflow\|caddy\|MLFLOW\|mlartifacts\|caddy_data" docker-compose.yml` returns 0
3. `grep -c "ENTRYPOINT" Dockerfile` returns 1
4. `grep -c "DOMAIN" .env.example` returns 0
5. All 3 new files in docker/ directory exist and have correct content
</verification>

<success_criteria>
- Dockerfile builds Python app with entrypoint-based volume seeding
- docker-compose.yml orchestrates exactly 4 services (postgres, api, worker, nginx) with 2 volumes (pgdata, models)
- docker/entrypoint.sh seeds models volume on first boot
- docker/nginx.conf routes /api/ to FastAPI and serves SPA at /
- docker/nginx.Dockerfile builds frontend in multi-stage (Node -> nginx)
- .env.example has 3 variables, no DOMAIN
- .dockerignore excludes frontend/dist
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/06-pipeline-and-deployment/06-02-SUMMARY.md`
</output>
